# Core LLM + orchestration
openai==2.32.0
python-dotenv==1.2.1

# RAG: Chroma vector store + OpenAI embeddings
chromadb==1.3.4
langchain-chroma==1.0.0
langchain-openai==1.0.2
langchain-core==1.4.9

# Web news search (free, no API key)
ddgs==9.14.4

# Web UI (also handles mic capture + audio playback in the browser)
gradio==6.12.0
